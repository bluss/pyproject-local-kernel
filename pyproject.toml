[project]
name = "pyproject-local-kernel"
version = "1.0.0"
description = "Root package"
dependencies = []
requires-python = ">= 3.8"

[tool.uv]
managed = true
dev-dependencies = []
python-preference = "only-managed"

[tool.uv.workspace]
members = []
exclude = ["x", "*", "**"]
